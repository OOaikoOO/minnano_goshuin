<div class="search_form">
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({"ユーザー名"=>"user", "神社仏閣の名前"=>"post", "所在地"=>"address"})%>
    <%= f.submit "検索" %>
  <% end %>
</div>
<div class="search_form">
  <%= form_with url: admin_search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({"ユーザー名"=>"user", "投稿"=>"post", "コメント"=>"comment"})%>
    <%= f.submit "検索" %>
  <% end %>
</div>
<h1>プロフィール編集</h1>
<%= form_with model: @user, url: user_path(@user), method: :patch do |f| %>
  <h4>名前</h4>
  <%= f.text_field :name %>
  
  <h4>Email</h4>
  <%= f.email_field :email %>
  
  <%= f.submit "変更を保存" %>
<% end %>